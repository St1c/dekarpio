import { SvgElementsClickListenerDirective } from './svg-elements-click-listener.directive';

describe('SvgElementsClickListenerDirective', () => {
  it('should create an instance', () => {
    const directive = new SvgElementsClickListenerDirective();
    expect(directive).toBeTruthy();
  });
});
