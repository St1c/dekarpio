import { SvgElementsRightClickListenerDirective } from './svg-elements-right-click-listener.directive';

describe('SvgElementsRightClickListenerDirective', () => {
  it('should create an instance', () => {
    const directive = new SvgElementsRightClickListenerDirective();
    expect(directive).toBeTruthy();
  });
});
