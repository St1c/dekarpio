import { SimulationValidityPipe } from './simulation-validity.pipe';

describe('SimulationValidityPipe', () => {
  it('create an instance', () => {
    const pipe = new SimulationValidityPipe();
    expect(pipe).toBeTruthy();
  });
});
