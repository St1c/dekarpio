import { SvgElementsHoverListenerDirective } from './svg-elements-hover-listener.directive';

describe('SvgElementsHoverListenerDirective', () => {
  it('should create an instance', () => {
    const directive = new SvgElementsHoverListenerDirective();
    expect(directive).toBeTruthy();
  });
});
